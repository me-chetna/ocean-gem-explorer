[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18.18.0"

# Optional redirects to handle SPA routing on Netlify
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
